% Optional override for the bundled font root.
% By default the system uses the repository-local assets/fonts directory.
% Copy this file to nextsystem.local.tex only if you want to override it.
\SetCatalogFontRoot{assets/fonts}
