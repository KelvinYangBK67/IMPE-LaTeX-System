% Pahlavi special builders.

\ExplSyntaxOn

\cs_new_protected:Npn \__font_define_special_pahlavi:
  {
    % fontspec does not accept Script=PsalterPahlavi as a standard Script key,
    % so shaping must be routed through RawFeature=script=phlp instead.
    \tl_clear:N \l__font_script_tl
    \tl_clear:N \l__font_language_tl
    \__font_special_use_generic:
  }

\ExplSyntaxOff
